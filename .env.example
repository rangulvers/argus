# Argus Environment Variables
# Copy this file to .env and customize
# IMPORTANT: Version 2.0+ requires secrets to be set via environment variables

# =============================================================================
# REQUIRED SECRETS (v2.0+)
# =============================================================================

# Session Secret - Used for signing session cookies
# REQUIRED in production mode (ARGUS_ENVIRONMENT=production)
# Generate with: python -c 'import secrets; print(secrets.token_urlsafe(32))'
ARGUS_SESSION_SECRET=your_secure_session_secret_here

# =============================================================================
# OPTIONAL SECRETS (only if features are enabled in config.yaml)
# =============================================================================

# Email Notifications - SMTP password
# Required only if email notifications are enabled
ARGUS_EMAIL_SMTP_PASSWORD=your_smtp_password_here

# Webhook Notifications - Webhook secret for HMAC signatures
# Required only if webhook notifications are enabled
ARGUS_WEBHOOK_SECRET=your_webhook_secret_here

# CVE Integration - NVD API key
# Required only if CVE vulnerability checking is enabled
# Get API key from: https://nvd.nist.gov/developers/request-an-api-key
ARGUS_CVE_API_KEY=your_nvd_api_key_here

# UniFi Integration - Controller credentials
# Required only if UniFi controller integration is enabled
ARGUS_UNIFI_PASSWORD=your_unifi_password_here
ARGUS_UNIFI_API_KEY=your_unifi_api_key_here

# Pi-hole Integration - API token
# Required only if Pi-hole integration is enabled
# Get token from: Pi-hole Admin > Settings > API > Show API token
ARGUS_PIHOLE_API_TOKEN=your_pihole_api_token_here

# AdGuard Integration - Admin password
# Required only if AdGuard Home integration is enabled
ARGUS_ADGUARD_PASSWORD=your_adguard_password_here

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Environment Mode
# Set to "production" to enforce strict security (requires ARGUS_SESSION_SECRET)
# Set to "development" to allow file-based secrets (NOT for production)
ARGUS_ENVIRONMENT=development

# Database
DATABASE_URL=sqlite:///./data/argus.db

# Web Server
WEB_HOST=0.0.0.0
WEB_PORT=8080

# Authentication (optional)
ENABLE_AUTH=false
AUTH_USERNAME=admin
AUTH_PASSWORD=changeme

# Network Scanning
SUBNET=192.168.1.0/24
SCAN_PROFILE=normal
PORT_RANGE=1-1000

# Logging
LOG_LEVEL=INFO

# =============================================================================
# MIGRATION FROM v1.x
# =============================================================================
# If upgrading from v1.x where secrets were in config.yaml:
# 1. Run the migration script: python migrate_secrets.py
# 2. The script will extract secrets and populate this .env file
# 3. Verify secrets are correct, then restart Argus
# 
# See docs/SECURITY_MIGRATION.md for detailed instructions
